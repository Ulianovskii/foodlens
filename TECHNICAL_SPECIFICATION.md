# Техническое задание на разработку Telegram-бота «FoodLens» (Ревизия 5)

## 1. Общее описание
FoodLens — интеллектуальный Telegram-бот для анализа питания по фотографиям. Бот использует AI для определения пищевой ценности блюд и предоставляет персонализированные рекомендации.

**Целевая аудитория:** Россия, мужчины/женщины 20–40 лет, интересующиеся здоровым питанием и контролем калорий

## 2. Текущий статус реализации

### ✅ Завершено:
- **Модуль 1**: Архитектура и настройка проекта
- **Модуль 2**: Базовый каркас бота (aiogram 3.x)
- **Модуль 3**: Система анализа изображений (US1) - **ПОЛНОСТЬЮ ГОТОВО**

### 🔥 АКТИВНАЯ РАЗРАБОТКА:
- **Модуль 4**: Система лимитов и профили пользователей - **ЧАСТИЧНО РЕАЛИЗОВАНО**

### 📋 В плане:
- Модуль 5: Промокоды и подписка
- Модуль 6: Конфигурация тарифов
- Модуль 7: Дневник питания (US2)
- Модуль 8: Профиль и цели (US4)
- Модуль 9: Статистика (US3)

## 3. Архитектура и стек
- **Backend:** Python + Aiogram 3.x
- **AI:** OpenAI GPT-4 Vision API
- **База данных:** PostgreSQL + asyncpg
- **Конфиги:** Python модули (тексты, промты)
- **Локализация:** Русский язык
- **Инфраструктура:** Docker, Makefile для управления

## 4. User Stories

### US1. Анализ блюда ✅ РЕАЛИЗОВАНО
**Пользовательский сценарий:**
- Отправляет фото еды или нажимает "📸 Анализировать еду"
- Получает меню анализа с кнопками "📊 Калорийность" и "👨‍🍳 Рецепт"
- Может уточнять детали текстовыми сообщениями
- Получает точный анализ с учетом всех ингредиентов

**Реализованные функции:**
- ✅ Интеграция с GPT-4 Vision API
- ✅ Система сессий с контекстом (1 фото + до 5 уточнений)
- ✅ Экономия токенов (фото → 1 запрос)
- ✅ Удобный UI с reply-кнопками
- ✅ Автоочистка неактивных сессий (1 час)
- ✅ Защита от сторонних тем
- ✅ Стандартные веса для бытовых мерок
- ✅ Форматированный вывод калорийности и рецептов

### US5. Монетизация и подписка 🔥 АКТИВНАЯ РАЗРАБОТКА
**Текущая реализация:**
- ✅ Базовая система лимитов (3 фото/день бесплатно, 10 фото премиум)
- ✅ Middleware для проверки лимитов перед обработкой фото
- ✅ Модель User с полями: subscription_type, subscription_until, daily_photos_used, total_photos_analyzed, last_reset_date
- ✅ PromoService для генерации и активации промокодов
- ✅ Админ-команды для тестирования: `/generate_promo`, `/reset_limits`, `/user_info`, `/promo_list`

**Планируемый UX:**
- Команда `/profile` с информацией о лимитах
- Кнопка "⭐️ Подписаться" в главном меню
- Подменю подписки: неделя/месяц/промокод
- Активация промокодов через интерфейс

### US2. Дневник питания 📋 В ПЛАНЕ
- Inline-кнопка «📝 Добавить в дневник» → сохранение в food_log
- Просмотр дневника командой /diary
- Управление записями (удаление)
- Ежедневные отчеты

### US3. Статистика 📋 В ПЛАНЕ
- Команда /stats с вариантами: «За день», «За неделю», «За месяц»
- Сравнение с целью по калориям
- Визуализация прогресса

### US4. Профиль и цели 📋 В ПЛАНЕ
- Команда /profile
- Информация о подписке и лимитах
- Установка целей по калориям
- Смена языка

## 5. База данных

### Реализованные модели:
```sql
users:
  user_id (BigInt), created_at (Timestamp), language (Varchar),
  subscription_type (Varchar), subscription_until (Timestamp),
  daily_photos_used (Integer), total_photos_analyzed (Integer),
  last_reset_date (Date), username (Varchar), first_name (Varchar), last_name (Varchar)

promo_codes:
  id (Serial), code (Varchar), promo_type (Varchar),
  is_used (Boolean), used_by (BigInt), used_at (Timestamp),
  created_at (Timestamp), expires_at (Timestamp)
```

### Планируемые расширения:
```sql
food_log:
  id (Serial), user_id (BigInt), dish_name (Varchar),
  calories (Integer), proteins (Real), fats (Real), carbs (Real),
  weight (Integer), created_at (Timestamp)

tariffs:
  name (Varchar), price_stars (Integer),
  duration_days (Integer), photos_per_day (Integer),
  features (JSONB), is_active (Boolean)
```

## 6. Система лимитов 🔥 АКТИВНАЯ РАЗРАБОТКА

### Текущая реализация:
```python
# Модель User
class User:
    def get_daily_limit(self) -> int:
        return 3 if self.subscription_type == "free" else 10
    
    def can_analyze_photo(self) -> bool:
        return self.daily_photos_used < self.get_daily_limit()

# Middleware проверки лимитов
class LimitMiddleware:
    async def __call__(self, handler, event, data):
        if event.photo:
            # Проверка лимитов пользователя
            # Блокировка при превышении
```

### Бесплатный тариф:
- 3 фото анализа в день
- Показ рекламных сообщений при превышении лимита
- Базовая функциональность анализа

### Премиум тарифы:
- 10 фото анализа в день  
- Без рекламы
- Доступ ко всем функциям анализа

## 7. Промокоды и подписка 🔥 АКТИВНАЯ РАЗРАБОТКА

### Текущая реализация:
```python
class PromoService:
    async def create_promo_codes(self, promo_type: str, count: int) -> List[str]
    async def activate_promo_code(self, code: str, user: User) -> bool
    async def get_all_promo_codes(self) -> List[PromoCode]
```

### Типы промокодов:
- `premium_week` - премиум на 7 дней
- `premium_month` - премиум на 30 дней

### Админ-команды для тестирования:
- `/generate_promo week 1` - создать промокод на неделю
- `/activate_promo CODE` - активировать промокод
- `/reset_limits [user_id]` - сбросить лимиты пользователя
- `/user_info [user_id]` - информация о пользователе

## 8. Технические детали

### Обработка изображений:
- Не храним фото на диске
- Используем base64 кодирование для GPT API
- Сессии в памяти с таймаутом 1 час

### Безопасность:
- Валидация входящих данных
- Защита от перегрузки API
- Логирование ошибок
- Система проверки прав администратора

### Производительность:
- Кэширование часто используемых данных
- Оптимизация запросов к GPT
- Мониторинг нагрузки

## 9. Критические задачи для завершения Модуля 4

### 🚨 СРОЧНО ТРЕБУЕТ ИСПРАВЛЕНИЯ:
1. **Дублирование роутеров** в bot.py - вызывает ошибку запуска
2. **Ошибки в middleware** - неправильная проверка лимитов

### 🎯 БЛИЖАЙШИЕ ЗАДАЧИ:
1. **Исправить критические ошибки** запуска бота
2. **Реализовать команду /profile** с информацией о лимитах
3. **Добавить кнопку "⭐️ Подписаться"** в главное меню
4. **Создать интерфейс активации промокодов** для пользователей
5. **Настроить систему оплаты Telegram Stars** для тарифов

## 10. Планы развития

### Ближайшие задачи:
1. 🔥 **Завершить Модуль 4** - исправить ошибки и доделать профиль
2. 🚧 **Реализовать Модуль 5** - интерфейс подписки и оплаты  
3. 📋 **Модуль 6** - конфигурация тарифов через отдельные файлы
4. 📋 **Модуль 7** - дневник питания
5. 📋 **Модуль 8** - профиль и цели
6. 📋 **Модуль 9** - статистика

### Долгосрочные планы:
- Интеграция с популярными трекерами калорий
- Расширенная аналитика питания
- Социальные функции (делиться результатами)
- Мобильное приложение

---

**Версия документа:** 5.0  
**Последнее обновление:** 2024  
**Статус проекта:** Активная разработка (исправление критических ошибок)  
**Репозиторий:** https://github.com/Ulianovskii/foodlens