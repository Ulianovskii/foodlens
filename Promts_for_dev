Мы с тобой разрабатываем бота по моему ТЗ.

Мой репозиторий - https://github.com/Ulianovskii/foodlens
**Ссылка на ТЗ:** TECHNICAL_SPECIFICATION.md  
**Ссылка на план:** DEVELOPMENT_PLAN.md
**Описание разработанных модулей:** Technical_doc.md

## Текущий модуль: Модуль 3 - Система анализа изображений (US1)
**Цель:** Реализовать анализ фотографий продуктов

## Архитектура проекта (ВАЖНО!):
- Структура: `app/` с подпакетами `handlers/`, `locales/`, `services/`
- Все тексты в системе локализации (`app/locales/ru/texts.py`)
- Используем существующие обработчики в `app/handlers/`
- Запуск через Makefile: `make run`, `make install`
- Конфигурация через `.env`

## Что уже реализовано (Модуль 2):
✅ Базовый каркас бота на aiogram 3.x  
✅ Обработчики команд: `/start`, `/help`, `/cancel`, `/analyze`, `/history`  
✅ Система локализации с русскими текстами
✅ Обработка фотографий (заглушка)
✅ Логирование и обработка ошибок
✅ Makefile для автоматизации

## Задача для Модуля 4:
Задача для Модуля 4 - Дневник питания (US2)
Цель: Реализовать систему сохранения анализов в дневник питания и управление записями.

📋 Задачи:
1. Модель данных для дневника

Создать модель FoodLog в базе данных

Поля: id, user_id, dish_name, calories, proteins, fats, carbs, weight, created_at

Интеграция с существующей PostgreSQL

2. Сохранение анализов

Добавить inline-кнопку "📝 Добавить в дневник" к результатам анализа

Сохранять данные из GPT анализа в дневник

Подтверждение пользователю об успешном добавлении

3. Просмотр дневника

Команда /diary или кнопка "📊 Мой дневник"

Показ записей за текущий день

Формат: список блюд с калориями и БЖУ

Итоги за день (сумма калорий, БЖУ)

4. Управление записями

Inline-кнопка "❌ Удалить" для каждой записи

Подтверждение удаления

Обновление списка после удаления

5. Интеграция с существующей системой

Использовать текущую систему локализации для текстов

Интегрировать с существующими обработчиками

Сохранить архитектуру кнопок и состояний

🎯 Требования к реализации:
Архитектура:

Использовать существующую структуру app/

Все тексты добавить в app/locales/ru/texts.py

Новые обработчики в app/handlers/diary_handler.py

Модели данных в app/models/

Пользовательский опыт:

После анализа показывать кнопку "Добавить в дневник"

Быстрый просмотр дневника без лишних шагов

Простое управление записями (удаление)

Технические детали:

Inline-кнопки с callback данными

Валидация данных перед сохранением

Обработка ошибок БД
Реализовать функционал анализа изображений вместо заглушки в `app/handlers/photo_handler.py`

Локальная папка: `/Users/andrew/Dev/foodlens`
Компьютер: MacBook M1 Pro

**Не создавай новую структуру файлов! Используй существующую архитектуру.**

# Добавить в app/locales/ru/texts.py
'add_to_diary': "📝 Добавить в дневник",
'my_diary': "📊 Мой дневник", 
'added_to_diary': "✅ Блюдо добавлено в дневник!",
'diary_empty': "📝 В вашем дневнике пока нет записей за сегодня.",
'diary_entries': "📊 Ваш дневник за сегодня:\n\n{entries}\n\n**Итого:** {total_calories} ккал",
'remove_entry': "❌ Удалить",
'confirm_remove': "❌ Удалить эту запись?",
'entry_removed': "✅ Запись удалена из дневника",

🔄 Workflow пользователя:
Анализ блюда → получает результат с кнопкой "Добавить в дневник"

Нажатие → блюдо сохраняется, показывается подтверждение

Просмотр дневника → команда /diary показывает все записи за день

Удаление → кнопка "Удалить" у каждой записи с подтверждением

Готов приступить к реализации Модуля 4? 🚀